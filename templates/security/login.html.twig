{% extends 'frontend/base-login.html.twig' %}

{% block body_id 'login' %}

{% block main %}

    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <form class="form-horizontal m-t-20" action="{{ path('security_login') }}" method="post">
                    <fieldset>
                        <legend><h4><i class="fa fa-lock" aria-hidden="true"></i> {{ 'title.login'|trans }}</h4></legend>

                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text" id="basic-addon1"><i class="mdi mdi-account"></i></span>
                            </div>
                            <input type="text" value="{{ last_username }}" id="username" name="_username" class="form-control form-control-md" placeholder="{{ 'label.username'|trans }}" aria-label="{{ 'label.username'|trans }}" aria-describedby="basic-addon1">
                        </div>

                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text" id="basic-addon2"><i class="mdi mdi-shield-outline"></i></span>
                            </div>
                            <input type="password" id="password" name="_password" class="form-control form-control-md" placeholder="{{ 'label.credential'|trans }}" aria-label="{{ 'label.credential'|trans }}" aria-describedby="basic-addon1">
                        </div>

                        <input type="hidden" name="_target_path" value="{{ app.request.get('redirect_to') }}"/>
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa-sign-in" aria-hidden="true"></i> {{ 'action.sign_in'|trans }}
                        </button>
                        <a href="{{ path('security_reset_password') }}" class="btn btn-link pull-right">
                            <i class="mdi mdi-account-key" aria-hidden="true"></i> {{ 'action.go_to_password_reset'|trans }}
                        </a>
                    </fieldset>
                </form>
            </div>
        </div>

        {#
        <div id="login-help" class="col-sm-12 mt-5">
            <h5>
                <i class="hidden-xs fa fa-long-arrow-left" aria-hidden="true"></i>
                {{ 'help.login_users'|trans }}
            </h5>

            <table class="table table-striped table-bordered table-hover">
                <thead>
                    <tr>
                        <th scope="col">{{ 'label.username'|trans }}</th>
                        <th scope="col">{{ 'label.credential'|trans }}</th>
                        <th scope="col">{{ 'label.role'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>john_user</td>
                        <td>kitten</td>
                        <td><code>ROLE_USER</code> ({{ 'help.role_user'|trans }})</td>
                    </tr>
                    <tr>
                        <td>jane_admin</td>
                        <td>kitten</td>
                        <td><code>ROLE_ADMIN</code> ({{ 'help.role_admin'|trans }})</td>
                    </tr>
                </tbody>
            </table>

            <div id="login-users-help" class="card card-default">
                <div class="card-body">
                    <p>
                        <span class="label label-success">{{ 'note'|trans }}</span>
                        {{ 'help.reload_fixtures'|trans }}<br/>

                        <code class="console">$ php bin/console doctrine:fixtures:load</code>
                    </p>

                    <p>
                        <span class="label label-success">{{ 'tip'|trans }}</span>
                        {{ 'help.add_user'|trans }}<br/>

                        <code class="console">$ php bin/console app:add-user</code>
                    </p>
                </div>
            </div>
        </div>
        #}
    </div>
{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {# show_source_code(_self) #}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{{ dump(error) }}
    <script>
        {% if error %}
        toastr.error('', '{{ error.messageKey|trans(error.messageData, 'security') }}');
        {% endif %}
    </script>
{% endblock %}
